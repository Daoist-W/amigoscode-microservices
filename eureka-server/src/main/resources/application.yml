server:
  port: 8761

spring:
  application:
    name: eureka-server
  spring:
    zipkin:
      baseUrl: http://localhost:9411/

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false